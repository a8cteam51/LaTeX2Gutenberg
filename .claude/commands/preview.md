Preview a LaTeX file in WordPress Playground.

Run `python preview.py $ARGUMENTS` from the project root to convert the LaTeX file and open the result in WordPress Playground in your browser.

If no file is specified, use `test_features.tex` as the default.
